# Tuple data structure
# Tuple can store any data Type
# Tuples are ordered collection of items
# tuple are immutable, once tuple is created, you cannot modify
# data inside tuple
# tuples are created via round brackets
# list are created via square brackets
# tuples are faster than list and generally used when is static or fixed.
example = ('one', 'two', 'three')
# no append, no insert, no pop , no remove

# days=('monday','tuesday')

# methods
# count, index,len, slicing

print(example[:2])
# not allowed 
# example[1]='Test'
print(example)
